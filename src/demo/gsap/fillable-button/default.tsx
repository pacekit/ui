import { FillableButton } from "@/components/gsap/fillable-button";

export const Demo = () => {
    return (
        <FillableButton variant="default">
            Hover me
        </FillableButton>
    );
};
